package qa.com.solid.single;

public class Spotify {
	
	public String playMusic(String music, String name) {
		return(name + " listening to " + music);
	}
	
	// Using a theme of your choice (Pizzas) 
	// Create a class with basic constructors, getters and setters
	// Create a separate class running a method that uses the original class' data
	// Make the other class return or syso "something" 

}
